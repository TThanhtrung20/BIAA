# Phụ thuộc của Bia. Cài trong môi trường ảo:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# LƯU Ý: Ollama KHÔNG phải gói pip. Cài riêng từ https://ollama.com rồi tải model:
#   ollama pull qwen2.5-coder:7b
#   ollama pull nomic-embed-text

# --- Soạn thảo Office (Word / Excel / PowerPoint) ---
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2

# --- Mascot 3D trên desktop (Qt/QML) ---
PySide6==6.11.1

# --- Giọng nói offline: STT (nghe) + TTS (nói) ---
faster-whisper==1.2.1
sounddevice==0.5.5
numpy==2.5.1
piper-tts==1.4.2

# --- Trí nhớ mạng nơ-ron lưu trong PostgreSQL (pgvector). Tùy chọn: ---
# nếu không kết nối được Postgres, Bia tự quay về lưu JSON.
psycopg2-binary==2.9.12
